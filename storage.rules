rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Restrict to authenticated users only
      allow read, write: if request.auth != null;
    }
  }
}
